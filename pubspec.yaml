name: get_tidy
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # -------------------------------------
  cupertino_icons: ^1.0.2
  fluttertoast: ^8.0.8
  get: ^4.6.1
  shared_preferences: ^2.0.11
  syncfusion_flutter_datepicker: ^19.4.38
  adobe_xd: ^2.0.0+1
  firebase_core: ^1.10.6
  firebase_auth: ^3.3.4
  syncfusion_flutter_calendar: ^19.4.38
  syncfusion_localizations: ^19.4.38+1
  flutter_signin_button: ^2.0.0
  firebase_analytics: ^5.0.2
  cloud_firestore: ^3.1.5
  google_sign_in: ^5.2.1
  google_sign_in_web: ^0.10.0+3
  table_calendar: ^3.0.3
  smart_select: ^4.3.2
  dio: ^4.0.4
  firebase_phone_auth_handler: ^0.0.8
  comment_box: ^0.0.16
  favorite_button: ^0.0.4
  flutter_rating_stars: ^1.0.3+4
  flutter_localizations:
    sdk: flutter
    version: ^0.0.0
  google_maps_flutter: ^2.0.3
  location: ^4.3.0
  # geolocation: ^1.1.2
  google_maps: ^5.3.0
  geocode: ^1.0.1
  # geolocator: ^8.0.1
  # custom_google_map_markers: ^0.0.1
  # -------------------------------------

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

# The following section is specific to Flutter.
flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/
    - assets/images/
    - assets/drawable-hdpi/
    - assets/drawable-ldpi/
    - assets/drawable-mdpi/
    - assets/drawable-xhdpi/
    - assets/drawable-xxhdpi/
    - assets/drawable-xxxhdpi/
  #   - images/a_dot_ham.jpeg
